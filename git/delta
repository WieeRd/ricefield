# vim: filetype=gitconfig

[core]
	pager = delta

[pager]
	diff = delta

[interactive]
	diffFilter = delta --color-only --features=interactive

[delta]
	navigate = true
	hyperlinks = true
	line-numbers = true
	side-by-side = false
